/* Author: Marcus Ross
 *         MR867434
 */

#define MAX_ROWS 100 /* the most allowed rows per table */
#define MAX_TABLES 10 /* the most allowed tables per database */
#define MAX_LEN_NAME 16 /* the longest allowed table name or column name */
#define MAX_COLS 10 /* the most allowed columns per table */